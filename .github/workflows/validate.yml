name: Validate benchmark

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate records, runners, and artifacts
        run: python3 scripts/validate_repository.py
